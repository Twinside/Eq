
#include <stdio.h>

    int
main(int argc, char **argv)
{
    int x;
    /* Eq:eval x = derivate( sqrt( x ^ 2 ) + 1, x ) */
    int y;
    /* Eq:format integrate( a, b,       */
    /* fromage * sin( camembert ) / 2, fromage )   */
    return 0;
}
